{% extends 'base.html.twig' %}

{% block title %}Eugeniu{% endblock %}

{% block body %}
    <h1>Eugeniu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eugeniu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eugeniu.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ eugeniu.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eugeniu2_index') }}">back to list</a>

    <a href="{{ path('app_eugeniu2_edit', {'id': eugeniu.id}) }}">edit</a>

    {{ include('eugeniu2/_delete_form.html.twig') }}
{% endblock %}
