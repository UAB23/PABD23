<form method="post" action="{{ path('app_blog_comment_delete', {'id': blog_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_comment.id) }}">
    <button class="btn">Delete</button>
</form>
