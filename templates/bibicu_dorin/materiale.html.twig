{% extends "/bibicu_dorin/base.html.twig" %}


{% block body %}
    <div class="w-4/5 m-auto text-center">
        <div class="py-12 border-b border-gray-200">
            <h1 class="text-6xl pt-12 pb-8">
                Materiale didactice
            </h1>
            <span class="font-light text-gray-500 italic">
                under construction
            </span>
        </div>

    <font size="50px">
    {% if app.user %}
    {% if app.user.roles[0] =='ROLE_ADMIN' or app.user.roles[0] =='ROLE_USER' %}
        <a href="{{ path('openMatDid', {id: "orar.pdf"}) }}">Orar</h1></a>
        <br><br>
        <a href="{{ path('openMatDid', {id: "Manual_C.pdf"}) }}"> Curs C#</a>
    {% else %}
    Pentru a vizualiza pagina trebuie să fiți logat!
    {% endif %}
    {% else %}
    Pentru a vizualiza pagina trebuie să fiți logat!
    {% endif %}

    <br><br>
    </div>
{% endblock %}
